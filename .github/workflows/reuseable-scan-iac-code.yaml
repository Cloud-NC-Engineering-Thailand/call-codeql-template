name: 'Call reusable workflow CodeQL iac Scan'
on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  call-workflow-scan-terraform:
    permissions:
      actions: read
      contents: read
      security-events: write
    uses: uoio1/reusable-workflow/.github/workflows/reuseable-scan-iac-code.yaml@master
    with:
      path: terraform-code-folder